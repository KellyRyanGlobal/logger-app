pipeline {
    agent any
    
    stages {
        stage('Build') {
            agent { 
                dockerfile true               
            }
            environment {

                DOCKER_USER = credentials('docker-username')
                DOCKER_PASS = credentials('docker-password')
            }

            steps {
                sh '''
                make -C src
                ls -l
                chmod +x build.sh
                ./build.sh 
                '''
            }
        }
        stage('Test') {
            steps {
                sh '''
                chmod +x test/functional/functional.s
                test/functional/functional.sh
                '''
            }
        }
     }
}
