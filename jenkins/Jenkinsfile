pipeline {
    agent { dockerfile true }
    environment {
        DOCKER_USER = credentials('docker-username')
        DOCKER_PASS = credentials('docker-password')
        APP_NAME = 'test_app'
        VERSION = '1.0'
    }
    stages {
        stage('Docker Build Test_App') {
            steps {
                sh 'ls'
                stash includes: '${APP_NAME}.exe', name: '${APP_NAME}'
            }
        }
        stage('Test out stash') {
            steps {
                unstash '${APP_NAME}'
                sh './${APP_NAME}.exe'
            }
        }
    }

}